# Industrial Knowledge Intelligence — core dependencies
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
pypdf>=4.0
rank-bm25>=0.2.2
networkx>=3.2
numpy>=1.26
python-dotenv>=1.0
pydantic>=2.6

# Optional — enables higher-quality LLM answers & entity extraction.
# The system runs fully WITHOUT these (offline extractive fallback).
# Install whichever provider you use (set LLM_PROVIDER in .env accordingly).
anthropic>=0.39
openai>=1.40

# Optional — enables dense semantic embeddings on top of BM25.
# Not required; retrieval works on BM25 + graph traversal without it.
# sentence-transformers>=2.6
